# 📰 Veritas News Analyzer

> **Повноцінний софт для аналізу цінності новин за Veritas Protocol**

Автоматичний аналізатор логічної цілісності новинних статей з підтримкою української та англійської мов.

---

## ✨ Можливості

- ✅ **Витягування тексту з URL** - автоматичний парсинг новинних сайтів
- ✅ **Багатомовність** - українська та англійська мови
- ✅ **Veritas LAC** - Logic Authenticity Check
- ✅ **Динамічний Slashing** - система репутації джерел
- ✅ **CLI інтерфейс** - зручна робота з командного рядка
- ✅ **Експорт результатів** - JSON формат для подальшого аналізу

---

## 🚀 Швидкий старт

### 1. Встановлення

```bash
# Клонуємо репозиторій
git clone https://github.com/your-username/veritas-news-analyzer.git
cd veritas-news-analyzer

# Встановлюємо залежності
pip install -r requirements.txt
```

### 2. Базове використання

#### Аналіз URL
```bash
python cli.py --url https://www.bbc.com/news/article-123
```

#### Аналіз тексту
```bash
python cli.py --text "Ваш текст для аналізу тут"
```

#### Аналіз з файлу
```bash
python cli.py --file article.txt --source "BBC News"
```

---

## 📖 Детальна документація

### Командний рядок (CLI)

#### Основні команди:

```bash
# Аналіз URL
python cli.py -u https://example.com/article

# Аналіз тексту з вказанням джерела
python cli.py -t "Текст статті" -s "Джерело"

# Аналіз з файлу
python cli.py -f article.txt

# Перевірка репутації джерела
python cli.py --reputation "bbc.com"

# Експорт історії
python cli.py --export history.json

# JSON вивід
python cli.py -u https://example.com/article --json

# Детальний вивід
python cli.py -u https://example.com/article --verbose
```

#### Параметри:

| Параметр | Короткий | Опис |
|----------|----------|------|
| `--url` | `-u` | URL новинної статті |
| `--text` | `-t` | Текст для аналізу |
| `--file` | `-f` | Файл з текстом |
| `--source` | `-s` | Назва джерела |
| `--reputation` | `-r` | Показати репутацію |
| `--export` | `-e` | Експорт історії |
| `--json` | `-j` | JSON формат виводу |
| `--verbose` | `-v` | Детальний вивід |

---

### Використання як модуля

```python
from app.analyzer import NewsAnalyzer

# Ініціалізація
analyzer = NewsAnalyzer()

# Аналіз URL
result = analyzer.analyze_url("https://www.bbc.com/news/article")
print(analyzer.generate_report(result))

# Аналіз тексту
result = analyzer.analyze_text(
    text="Ваш текст тут",
    source="Custom Source"
)

# Перевірка репутації
reputation = analyzer.get_source_reputation("bbc.com")
print(f"Reputation: {reputation}")

# Експорт історії
analyzer.export_history("my_history.json")
```

---

## 📊 Інтерпретація результатів

### Індекс ентропії (0.0 - 1.0)

- **0.0 - 0.2** 🟢 Високоякісний логічний сигнал
- **0.2 - 0.4** 🟡 Помірна риторика
- **0.4 - 0.6** 🟠 Підвищена демагогія
- **0.6 - 1.0** 🔴 Високий рівень шуму

### Статуси

| Статус | Репутація | Значення |
|--------|-----------|----------|
| `TRUSTED` | ≥ 0.85 | Надійне джерело |
| `MONITORED` | 0.60-0.84 | Під спостереженням |
| `WARNING` | 0.40-0.59 | Попередження |
| `REJECTED` | < 0.40 | Не рекомендується |

---

## 🧪 Приклади виводу

### Українська мова

```
╔═══════════════════════════════════════════════════════════════╗
║           VERITAS PROTOCOL - АНАЛІЗ НОВИНИ                    ║
╚═══════════════════════════════════════════════════════════════╝

📰 ЗАГОЛОВОК: Важлива новина дня
🌐 ДЖЕРЕЛО: example.com
📅 ЧАС АНАЛІЗУ: 2026-01-26T15:30:00

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 АНАЛІЗ VERITAS PROTOCOL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 Індекс ентропії: 0.234
📈 Репутація джерела: 0.78
🏷️  Статус: MONITORED
💬 Вердикт: Помірна кількість риторики

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 РЕКОМЕНДАЦІЇ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ Рекомендується перевірка фактів

✓ Дія: Використовувати
✓ Рівень довіри: Високий
✓ Критичне мислення: Низький
```

---

## 🔧 Розширені можливості

### Власні маркери

Ви можете додати власні маркери ентропії в `app/translator.py`:

```python
NOISE_MARKERS = {
    'uk': {
        "етично", "необхідно", "важливо",
        # Додайте свої тут
        "ваш_маркер_1", "ваш_маркер_2"
    }
}
```

### Інтеграція в власний проект

```python
from app.analyzer import NewsAnalyzer

class MyNewsApp:
    def __init__(self):
        self.analyzer = NewsAnalyzer()
    
    def check_article(self, url):
        result = self.analyzer.analyze_url(url)
        
        if result['veritas_analysis']['status'] == 'REJECTED':
            self.block_article(url)
        
        return result
```

---

## 🧑‍💻 Розробка

### Структура проекту

```
veritas-news-analyzer/
├── app/
│   ├── __init__.py
│   ├── analyzer.py      # Головна логіка
│   ├── scraper.py       # Витягування тексту
│   └── translator.py    # Мультимовність
├── cli.py               # CLI інтерфейс
├── requirements.txt
└── README.md
```

### Запуск тестів

```bash
# Тест scraper
python app/scraper.py

# Тест translator
python app/translator.py

# Тест analyzer
python app/analyzer.py
```

---

## 📝 Ліцензія

Veritas Ethical License v1.0 - використання дозволене для прозорих, етичних цілей.

**Заборонено:**
- Маніпуляція інформацією
- Surveillance без згоди
- Порушення substrate-agnostic principles

---

## 🤝 Контрибуція

Вітаються внески, що відповідають принципам Veritas:

1. Source-agnostic evaluation
2. Логічна цілісність над авторитетом
3. Прозорість і відтворюваність

---

## 🌐 Посилання

- **Original Veritas Protocol:** [github.com/Architekt-future/veritas-protocol](https://github.com/Architekt-future/veritas-protocol)
- **Issues:** [GitHub Issues](https://github.com/your-username/veritas-news-analyzer/issues)
- **Документація:** [Zenodo](https://zenodo.org/records/18360722)

---

**Made with 💠 Veritas Protocol v7.1**
